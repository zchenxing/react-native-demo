import { StyleSheet } from "react-native";

export const themeColor = '#69BECB'
export const themeLightColor = '#acd9df'

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
    }
})
