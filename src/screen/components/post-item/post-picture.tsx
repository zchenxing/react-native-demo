import React from "react";
import { Text, View } from "react-native";
import {Image} from 'react-native-elements'

const PostPicture: React.FC = (props) => {
    return (
        <View>
            <Text>PostPicture</Text>
        </View>
    );
};

export default PostPicture;
