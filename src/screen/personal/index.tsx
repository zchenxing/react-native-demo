import React from "react";
import { Text, View } from "react-native";

const PersonalScreen: React.FC = (props) => {
    return (
        <View>
            <Text>PersonalScreen</Text>
        </View>
    );
};

export default PersonalScreen;
